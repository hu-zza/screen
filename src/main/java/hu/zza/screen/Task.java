package hu.zza.screen;

import java.time.LocalDateTime;

public class Task {

    /**
     * Returns the count of the seconds between two instants (both excluded)
     *
     * @param from start instant (exclusive)
     * @param to   end instant (exclusive)
     * @return the count of the seconds between the two instants (both excluded)
     */
    public static long getSecondsBetween(LocalDateTime from, LocalDateTime to) {
        return 0;
    }
}
